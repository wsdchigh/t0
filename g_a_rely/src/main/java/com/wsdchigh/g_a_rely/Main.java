package com.wsdchigh.g_a_rely;

/*
 *  全局依赖模块
 *  <li>    所有的插件均需要变异这个模块
 *
 *  <li>    配置全局的架包
 *  <li>    配置全局的so包
 *  <li>    配置全局的静态资源文件
 *          <li>    为了避免可能发送的资源错误
 *          <li>    尽可能实现以下的要求
 *                  <li>    服务端下发统一的Map表，存放所有的文本类型数据
 *                  <li>    服务端下发统一的Map表，配置统一的url
 *                  <li>    避免从资源文件中读取数据(减少读取量)
 */
public class Main {
}
